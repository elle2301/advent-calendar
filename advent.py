import streamlit as stimport jsonimport osst.set_page_config(page_title="Adventskalender", layout="wide")# ========== JSON LADEN / ERSTELLEN ==========SAVE_FILE = "doors.json"def load_doors():    if os.path.exists(SAVE_FILE):        with open(SAVE_FILE, "r") as f:            return set(json.load(f))    else:        return set()def save_doors(open_doors):    with open(SAVE_FILE, "w") as f:        json.dump(list(open_doors), f)# Geöffnete Türen aus Datei ladenopen_doors = load_doors()# In Session übernehmenif "open_doors" not in st.session_state:    st.session_state.open_doors = open_doors# ========== WEIHNACHTSSTIL ==========st.markdown("""<style>    body {        background-color: #f7f3e9;    }    .open-box {        background-color: #fff3cd;        border: 3px solid gold;        padding: 18px;        border-radius: 12px;        margin-top: 10px;        font-size: 42px;        text-align: center;        box-shadow: 0 0 8px gold;    }</style>""", unsafe_allow_html=True)# ========== HEADER ==========st.markdown("""<h1 style='text-align:center; color:#d62828; font-size: 60px;'>🎄 Adventskalender 2024 🎄</h1><p style='text-align:center; font-size: 22px; color:#444;'></p>""", unsafe_allow_html=True)# Placeholder-Emojis (später austauschbar)emojis = {i: "🎁" for i in range(1, 25)}# ========== LAYOUT ==========cols_per_row = 6for row in range(4):    cols = st.columns(cols_per_row)    for idx, col in enumerate(cols):        door = row * cols_per_row + idx + 1        if door > 24:            continue        with col:            is_open = door in st.session_state.open_doors            # Türchen-Button            if st.button(f"Tür {door}", key=f"door{door}"):                st.session_state.open_doors.add(door)                save_doors(st.session_state.open_doors)   # <-- SPEICHERN!                is_open = True            # Anzeige des Inhalts            if is_open:                st.markdown(f"<div class='open-box'>{emojis[door]}</div>",                            unsafe_allow_html=True)